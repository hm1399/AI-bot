name: ai_bot_app
description: Flutter frontend aligned with the AI-bot app-v1 backend contract.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  hooks_riverpod: ^2.4.0
  go_router: ^12.1.3
  http: ^1.1.0
  intl: 0.20.2
  shared_preferences: ^2.2.0
  web_socket_channel: ^2.4.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
