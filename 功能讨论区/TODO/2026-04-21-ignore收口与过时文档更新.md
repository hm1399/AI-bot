# 2026-04-21 ignore收口与过时文档更新

## 任务目标

- [x] 按用户要求保留本地 `venv/` 与 `.manager/`，但不再上传到 GitHub。
- [x] 收口可能带配置或密钥风险的本地生成文件的 ignore 策略。
- [x] 更新 `app/README.md`，使其与当前 6 个主区和真实 Flutter 功能对齐。
- [x] 更新 `app/DESIGN.md`，使其与当前产品结构和壳层口径对齐。
- [x] 更新 `BACKEND_INTEGRATION_GUIDE.md`，去掉旧 React / 旧端口 / 旧接口口径，并统一为英文说明。

## 调研范围

- [x] `.gitignore` 与当前被 git 跟踪的本地运行态文件
- [x] `app/lib/config/routes.dart`、主要 screens / widgets / providers
- [x] `server/main.py`、`server/bootstrap.py`、`server/services/app_runtime.py`、`server/config.yaml`
- [x] 三份过时文档的当前内容与真实实现差异

## 输出要求

- [x] 不删除用户点名的文档，只更新为当前真实说明。
- [x] 不删除本地 `venv/`、`.manager/`，只通过 ignore 和取消跟踪避免继续上传。
- [x] 不暴露任何实际 API key / token 值。
- [x] 完成后先向用户汇报文件变更，等待确认后再 git 提交。

## 约束

- [x] 按当前用户约束，不跑测试。
- [x] 先完成文档与 ignore 收口，再等待用户确认后执行 git 提交。

## 状态

- [x] 2026-04-21 已完成 ignore 收口与三份过时文档更新；本地 `venv/`、`.manager/` 保留，Git 跟踪已移除，测试按用户要求未执行。
