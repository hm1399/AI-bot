# 任务

让 `app/` 下的 Flutter 前端可以在 Web 端运行。

# 目标

- 确认现有 Flutter 工程的 Web 入口和依赖状态
- 找出阻塞 Web 运行或构建的兼容性问题
- 只修改与 Web 兼容直接相关的前端代码和配置
- 完成一次可复现的验证，并整理运行方式

# 执行步骤

1. 检查 `app/` 工程结构、依赖和平台相关代码。
2. 运行 Web 构建或分析，收集具体报错。
3. 修复 Web 平台下的路由、存储、网络或平台 API 兼容问题。
4. 重新验证 Web 构建结果。
5. 用 git 提交本次任务修改。
