# 2026-04-08 demo 固件切换到桌面麦克风代采与喇叭回放实施计划

## 目标

- 把 `firmware/arduino/demo/demo.ino` 从“设备本地麦克风直采”改成“设备触摸只发 `long_press / long_release`，由电脑麦克风代采”。
- 同时把服务端下发的 `audio_play + PCM 二进制 + audio_play_end` 接到硬件喇叭回放链路。
- 保留现有屏幕状态、文字显示、WiFi/WebSocket 连接体验。

## 调研结论

- 当前 `demo.ino` 仍在本地初始化 `INMP441`，并在触摸按住时持续上传 PCM。
- 服务端 `DeviceChannel` 已支持：
  - 接收 `touch_event.long_press / long_release`
  - 下发 `audio_play`
  - 通过 WebSocket 二进制帧推送 `16kHz / 16bit / mono PCM`
  - 下发 `audio_play_end`
- 现有功放测试稿 `firmware/arduino/test4_tts/test4_tts.ino` 已确认可用引脚：
  - `BCLK = IO17`
  - `LRC = IO18`
  - `DOUT = IO21`
  - `SD_MODE = IO2`

## 本次只改的文件

- 修改：`/Users/mandy/Documents/GitHub/AI-bot/firmware/arduino/demo/demo.ino`

## 不在本次修改范围

- 不改前端
- 不改后端协议
- 不恢复设备本地麦克风采音
- 不新增自动化测试，不跑测试命令
- 不顺手改其他固件草图

## 实施步骤

- [x] 删除 `INMP441` 初始化、I2S 录音、PCM 上行发送、`audio_end` 上报逻辑
- [x] 新增 `touch_event` 发送函数，按下发 `long_press`，松开发 `long_release`
- [x] 接入 `MAX98357A` 本地回放初始化
- [x] 接收 `audio_play` 时进入播放态并清空播放缓冲
- [x] 接收 WebSocket 二进制帧时写入喇叭播放缓冲
- [x] 接收 `audio_play_end` 时结束播放并回到空闲态
- [x] 保留现有屏幕状态与文字反馈
- [x] 完成后回填本计划完成状态

## 当前结果

- `demo.ino` 已切到“设备只发触摸事件，电脑端代采音频”的链路。
- `demo.ino` 已接入 `MAX98357A` 本地喇叭播放服务端回传的 PCM 音频。
- 本次未运行测试或编译验证，符合当前“先不跑测试”的约束。

## 风险点

- `demo.ino` 现在只引用了 `TFT_eSPI / ArduinoJson / WebSockets`，接入喇叭回放后会新增音频播放依赖，需与现有 Arduino 环境兼容。
- 如果当前板子上的功放线序和测试稿不一致，会导致有链路但无声。
- 如果固件版本和文档假设不一致，设备按下触摸后可能只显示状态变化，不会真正触发电脑麦克风代采。
