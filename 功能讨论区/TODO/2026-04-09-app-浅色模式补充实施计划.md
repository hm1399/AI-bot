# app 浅色模式补充实施计划

## Goal

在不扩展设置页功能的前提下，为 `app` 补一套尽量贴近 Linear 气质的浅色模式，并保持现有深色模式不退化。

## Scope

- [x] 为 `Linear` 主题补充浅色 token。
- [x] 为 `LinearTheme` 增加 `light()`，保持深浅模式的品牌色、层级、边框语言一致。
- [x] 在 `app/lib/main.dart` 切到 `ThemeMode.system`，让系统主题自动决定浅深色。
- [x] 不新增设置项、不新增按钮、不跑测试；仅做本次用户指定修改。
