# W1-B 最终规格审查

> 审查日期：2026-03-26
> 审查范围：`183e078c48adacebc8c5f34a2697469604dee5c5`、`056ac6abc09008de507439ebd549c0367ab8a6f0` 及其中间提交形成的最终结果
> 目标：判断 W1-B 是否满足限定修改范围与功能规格

---

## 检查项

- 允许修改文件范围
- provider timeout/error contract
- LiteLLM 单次请求总超时与错误分类
- config / bootstrap timeout 接线、默认值与校验
- 规格要求测试是否覆盖且可运行

## 结论

- 当前结论：FAIL

## 关键依据

### 1. 最终结果仍包含超出允许范围的文件变更

- 最终 diff 除允许文件外，还包含：
- `功能讨论区/TODO/2026-03-26-05A-W1-A-共享原子写工具与SessionManager-TODO.md`
- 该文件由中间提交 `1997f987c3b9004c7536dc95f35dd16f4aff9dba` 删除，不在 W1-B 允许修改列表内。

### 2. bootstrap timeout 测试在当前仓库环境中不能通过

- `server/tests/test_bootstrap_provider_timeout.py` 在模块顶层直接 `from bootstrap import create_agent`
- 导入 `bootstrap` 会继续导入 `services.app_runtime -> channels.device_channel -> services.asr`
- 当前仓库虚拟环境缺少 `numpy`，执行
- `venv/bin/python -m unittest server.tests.test_bootstrap_provider_timeout -q`
- 会报 `ModuleNotFoundError: No module named 'numpy'`
- 因此这条为 W1-B 新增的 bootstrap 覆盖当前不可运行，不能算稳定满足规格

### 3. 其余规格点基本已落地

- `server/nanobot/providers/base.py` 已增加结构化 `LLMError` 与 `LLMResponse.error`
- `server/nanobot/providers/litellm_provider.py` 已使用 `asyncio.wait_for(...)` 实现单次总超时，并区分 `timeout` / `provider_error`
- `server/config.py`、`server/config.yaml`、`server/bootstrap.py` 已接入 `provider_timeout_seconds` 默认值、校验与 bootstrap 传递
- `server/tests/test_litellm_provider.py` 与 `server/tests/test_config.py` 在当前环境可通过
