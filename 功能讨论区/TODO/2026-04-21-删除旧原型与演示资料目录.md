# 2026-04-21 删除旧原型与演示资料目录

## 任务目标

- [x] 调研 `App Builder (Copy)/`、`APP for BASS 44/`、`DEMO/`、`Project_proposal/` 是否影响当前主程序运行。
- [x] 在确认不影响 `app/`、`server/`、`firmware/`、`manager.sh` 主链路后删除上述目录。
- [x] 修正当前仓库索引文档中对已删除目录的现行说明，避免留下误导性入口。

## 调研范围

- [x] `app/`、`server/`、`firmware/`、`manager.sh` 的运行时引用
- [x] `README.md`、`CLAUDE.md` 等当前仓库索引文档
- [x] 四个待删除目录的内容与用途

## 输出要求

- [x] 仅在确认不影响主程序运行时删除目录
- [x] 不跑测试，等待用户后续指示
- [x] 完成后先汇报变更，再执行 git 提交

## 状态

- [x] 2026-04-21 已确认这 4 个目录不在当前 `app/`、`server/`、`firmware/`、`manager.sh` 主运行链路中，并已删除目录与同步修正文档索引；测试按用户要求未执行。
