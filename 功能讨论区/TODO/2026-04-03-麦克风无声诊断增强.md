# 2026-04-03 麦克风无声诊断增强

## 任务目标

在现有 `firmware/arduino/test12_mic_diag/test12_mic_diag.ino` 上增加更直观的芯片信息和运行状态打印，帮助定位“麦克风没收到声音”是芯片工作异常、系统状态异常，还是麦克风链路异常。

## 计划改动

1. 启动时打印芯片型号、版本、核心数、CPU频率、SDK版本、Flash、PSRAM、Heap、MAC、复位原因。
2. 运行时打印当前工作状态，包括活跃声道、音量、峰值、噪声基线、信号状态、可用内存。
3. 在草图里加入大白话提示，告诉用户每类数据代表什么、什么现象算不正常。
4. 用本机 Arduino IDE 内置 `arduino-cli` 编译验证。
5. 使用 `git` 提交本次修改。

## 执行结果

- 已修改 `firmware/arduino/test12_mic_diag/test12_mic_diag.ino`
- 已新增启动信息：
  - 芯片信息
  - 复位原因
  - 数据判读提示
- 已新增运行时工作状态信息：
  - `activeSlot`
  - `avg`
  - `peak`
  - `noise`
  - `signal`
  - `heap`
  - `psram`
- 已验证编译命令：

```bash
'/Applications/Arduino IDE.app/Contents/Resources/app/lib/backend/resources/arduino-cli' \
  compile --fqbn esp32:esp32:esp32s3 \
  --build-path /tmp/ai-bot-test12-mic-status \
  /Users/mandy/Documents/GitHub/AI-bot/firmware/arduino/test12_mic_diag
```

- 编译结果：
  - `Sketch uses 309031 bytes (23%) of program storage space`
  - `Global variables use 22556 bytes (6%) of dynamic memory`
