# Agenda 日历尺寸调整计划

**目标:** 将 `Agenda` 页面中的月历视觉尺寸缩小约 `1/3`，并在后续微调中进一步缩小且居中，避免桌面端占位过大，同时不破坏现有 Linear 风格和当天行程列表的可读性。

## 任务

- [x] 调研 `AgendaScreen` 与 `PlanningMonthCalendar` 的布局关系，确认月历尺寸主要受外层宽度约束影响。
- [x] 在 `Agenda` 页面为月历增加桌面端宽度约束，使其在宽屏下缩小到约当前尺寸的 `2/3`。
- [x] 保持中小屏幕下的可用性，不把月历压缩到难以点击。
- [x] 根据验收反馈继续缩小月历，并调整为卡片内居中展示。
