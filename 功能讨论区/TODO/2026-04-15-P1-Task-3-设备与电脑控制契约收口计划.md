# 2026-04-15 P1 Task 3 设备与电脑控制契约收口计划

## 背景

- 任务范围仅限 `server/services/computer_control/policies.py`、`server/channels/device_channel.py`、`server/models/protocol.py`、`firmware/arduino/demo/demo.ino`、`firmware/arduino/demo/face_display.cpp`。
- 如确有必要，可补 `app/lib/widgets/home/device_card.dart` 与 `app/lib/models/home/runtime_state_model.dart`，但仅处理 capability / validity 相关语义。
- 本轮禁止修改 `server/config.py`、`server/services/app_runtime.py`。
- 本轮禁止跑测试、禁止 git commit。

## 文件职责

- `server/services/computer_control/policies.py`
  - 收紧 `computer_control.allowed_scripts` 的解析契约，移除 list 形式静默忽略路径。
- `server/channels/device_channel.py`
  - 收口 `text_reply` / `display_update` 职责。
  - 为状态栏 / 电量 / 天气补 capability / validity 语义，并把 demo 固件的 unavailable 语义传到 runtime snapshot。
- `server/models/protocol.py`
  - 明确设备 WS 协议中回复文本、短 hint、状态栏 capability / validity 的契约常量与注释。
- `firmware/arduino/demo/demo.ino`
  - demo 固件不再伪造状态栏 / 电量 / 天气真实值，改为上报 unavailable 语义并按新协议消费服务端消息。
- `firmware/arduino/demo/face_display.cpp`
  - 仅在需要时配合 demo 状态栏空实现的 unavailable 语义，不扩展 UI。
- `app/lib/models/home/runtime_state_model.dart`
  - 如后端新增 capability / validity 字段，补解析模型。
- `app/lib/widgets/home/device_card.dart`
  - 如确有必要，避免把 unavailable 能力渲染成正式可见指标。

## 执行步骤

- [x] 复核现有后端 / 固件 / 前端契约与主线程脏文件边界。
- [x] 收紧 `allowed_scripts` 为 object-only，并保证错误路径不再静默吞掉。
- [x] 区分 `text_reply` 与 `display_update`，仅让 `display_update` 承载短 hint。
- [x] 为状态栏 / 电量 / 天气补 capability / validity 语义，并让 demo 固件上报 unavailable。
- [x] 如必须，最小化补前端 capability / validity 展示语义。
- [x] 回填本计划完成标记，并整理主线程需补的 config / runtime / app 接线与风险。

## 备注

- 本轮只做契约收口，不顺手改其他 UI / 架构问题。
- 测试与提交均按用户要求跳过。
- 已按要求未跑测试，结果仅基于静态复核与 diff 检查。
