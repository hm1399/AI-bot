# 2026-04-21 README 英文更新与全项目调研

## 任务目标

- [x] 以仓库当前实际实现为准，调研根目录、前端、后端与固件相关目录。
- [x] 将根目录 `README.md` 更新为英文说明，去掉当前中文旧口径。
- [x] 保持本轮只修改文档，不改动前后端实现与现有 UI/UX。
- [x] 将本轮顺带发现的旧设计 / 过时文档 / 性能风险记录到 `功能讨论区/TODO/waitlist.md`。

## 调研范围

- [x] 复核仓库级入口与说明：`README.md`、`manager.sh`、`CHANGELOG.md`、`功能讨论区/TODO/`
- [x] 复核前端真实结构：`app/pubspec.yaml`、`app/lib/config/routes.dart`、主要 screens / widgets / providers
- [x] 复核后端真实结构：`server/main.py`、`server/bootstrap.py`、`server/services/app_runtime.py`、`server/config.yaml`
- [x] 复核固件与硬件相关目录：`firmware/arduino/`、仓库顶层目录结构
- [x] 汇总 subagent 并行调研结果，统一口径后写入 README

## 输出要求

- [x] README 全文使用英文，不保留中文主说明。
- [x] 内容必须反映当前真实运行方式，包括 Flutter app、Python backend、可选 WhatsApp bridge、manager 工作流与硬件/固件目录。
- [x] 不把未确认或过时能力写成当前主路径。
- [x] 完成后先向用户汇报结果，等待确认后再执行 git 提交。

## 约束

- [x] 按用户要求，本轮不跑测试。
- [x] 按仓库约束，本轮使用 subagents 并行调研前端、后端与仓库级文档。
- [x] 完成后将本计划内已完成事项全部打勾。

## 完成说明

- 已完成根目录 `README.md` 英文化重写，口径改为当前真实主路径：`app/ + server/ + firmware/arduino/demo/`。
- 已将本轮额外发现同步更新到 `功能讨论区/TODO/waitlist.md`。
- 按当前用户约束，未跑测试；git 提交等待用户确认文件后再执行。
