# 2026-04-03 Arduino 麦克风测试草图

## 任务目标

在 `firmware/arduino` 下新增一个用于当前硬件板卡的麦克风测试文件，优先验证板载 `INMP441` 麦克风链路是否正常。

## 当前已知硬件信息

- 主控：`ESP32-S3-WROOM-1-N16R8`
- 麦克风：`INMP441`
- 网络标号：
  - `I2S_MIC_SCK`
  - `I2S_MIC_WS`
  - `I2S_MIC_SD`

## 执行步骤

1. 核对现有 `firmware/arduino` 测试草图与近期提交，避免重复造轮子。
2. 和用户确认麦克风测试的输出目标与成功标准。
3. 给出可选实现方案并确定最终测试草图设计。
4. 在 `firmware/arduino` 下新增测试草图并做本地验证。
5. 使用 `git` 提交本次任务成果。

## 已确认设计

- 新草图路径：`firmware/arduino/test12_mic_diag/test12_mic_diag.ino`
- 目标硬件引脚：`SCK=IO14`、`WS=IO15`、`SD=IO16`
- 输出内容：
  - `slot0/slot1` 平均绝对值
  - `slot0/slot1` 峰峰值
  - 自动选择更活跃的 slot
  - 噪声基线和 `silence / weak / active` 状态
  - ASCII 音量条
  - 串口命令 `h / p / r`

## 验证结果

- 已找到本机 Arduino IDE 内置 `arduino-cli`
- 已用 `ESP32S3 Dev Module` 对新草图完成编译验证
- 编译输出：
  - `Sketch uses 305671 bytes (23%) of program storage space`
  - `Global variables use 22516 bytes (6%) of dynamic memory`
