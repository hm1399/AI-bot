# 2026-04-06 总 TODO 清单：功能实现、优化与硬件协同

## 文档前提

- 基线文档：`功能讨论区/2026-04-06-前后端功能实现进度全面调研.md`
- 本次复核代码范围：
  - 前端：`app/`
  - 后端：`server/`
  - 固件：`firmware/arduino/`
- 当前口径不是继续对比旧方案，而是看当前 Nanobot 路线与目标功能是否对齐。
- 新增约束直接纳入主线：
  - `硬件麦克风当前不可用`
  - `对外交互仍保留“按住设备说话”`
  - `当前实际语音采集由电脑麦克风承担，设备负责触发与收音状态反馈`
  - `硬件麦克风修复改为并行恢复项，不再阻塞主功能闭环`

## 当前进度快照

### 前端当前进度

已完成：

- Flutter 工程主骨架已成立，已具备 `Connect / Home / Chat / Tasks / Control Center / Settings / Demo Mode` 页面。
- 已接入 `bootstrap / runtime / sessions / settings / tasks / events / notifications / reminders / device / app events websocket` 主协议。
- 已有本地连接保存、事件流重连、`last_event_id` 恢复、Demo Mode、本地 provider 状态管理。

当前缺口：

- `app/lib/services/chat/voice_capture_service.dart` 仍是 stub，当前“按住设备说话 -> 电脑麦克风代采”的真实链路还没落地，电脑麦克风和手机麦克风都没有真实采集实现。
- `voiceAvailableProvider` 依据后端 capability 判定可用，但真实语音采集未落地，`按住设备说话` 相关 UI 与真实能力不一致。
- 多会话虽然 provider 已有 `selectSession()`，但 `ChatScreen` 没有完整的会话切换/新建入口。
- `Tasks / Events / Notifications / Reminders` 目前主要还是读展示，页面层 CRUD 不完整。
- `DeviceService.sendCommand()` 已有，控制中心页面还没做成真正可操作的设备控制面板。
- 连接页明确保留了 `mDNS / zeroconf` 为后续事项。
- 自动化验证很弱，当前只有一个启动级 `widget_test`。
- 本机当前 Flutter/Dart 版本不满足 `app/pubspec.yaml` 中 `sdk: ^3.11.1`，前端测试与分析无法在当前环境完成。

### 后端当前进度

已完成：

- `server/services/app_runtime.py` 已提供完整 `app-v1` HTTP + WebSocket 接口主干。
- Sessions、Messages、Settings、Tasks、Events、Notifications、Reminders、Runtime、Device、Capabilities 已接入。
- App 鉴权、事件流 replay、聊天入队与回流、设备命令入口、设备状态快照已具备。
- `server/services/app_api/resource_service.py` 已有本地 JSON 持久化 CRUD。
- `server/tests` 当前可跑通，既有调研结论为 `37 passed`。

当前缺口：

- 当前没有“电脑麦克风实时采集”的正式接入链路。
- `server/tools/test_client.py` 只能发送音频文件或模拟事件，不是实时电脑麦克风采集工具。
- `reminders` 目前是 CRUD 数据，不是实际调度执行系统。
- `wake_word`、`auto_listen` 目前主要停留在设置字段，没有真正检测或状态机闭环。
- 人格系统、场景模式、外部日历同步、手机推送通知都还没有产品级实现。
- `server/services/app_runtime.py` 继续膨胀，后续维护成本会越来越高。

### 硬件 / 固件当前进度

已完成：

- `firmware/arduino/demo/demo.ino` 仍保留 WiFi、`/ws/device`、屏幕显示、时间状态栏、天气状态栏、触摸按住交互与状态反馈的主结构。
- `server/channels/device_channel.py` 已具备设备状态机、触摸事件、摇一摇事件、音频接收、ASR、TTS 下发、设备命令下发。
- 硬件仍可作为“按住设备说话”的交互门面、在线状态载体、显示面板和物理交互扩展端。

当前缺口：

- 设备端 demo 对服务端返回的二进制音频仍是忽略处理，TTS 播放链路没有闭环。
- demo 固件没有完整 shake 上报和互动玩法闭环。
- 当前最关键的新现实约束是：`硬件麦克风已不可作为当前主路线输入源`。
- 当前应把设备定义为前台交互入口和状态反馈承载端，实际采音由电脑麦克风代采，不能再默认“设备自己录音”。

## 总体目标重排

现阶段建议把总体目标拆成两条线并行推进：

### 主交付线

- 用 `按住设备说话 + 电脑麦克风代采 + 当前后端 + 现有设备反馈` 先完成真实语音闭环。
- 先做到“用户按住设备后，设备能进入收音态，系统能稳定识别、能返回、能执行、能让用户在设备上感知到结果”。

### 恢复与增强线

- 修复硬件麦克风。
- 补回设备播放、摇一摇、物理交互和更完整的桌面摆件能力。

## P0：必须先完成的闭环 TODO

### P0-1 设备交互保留、电脑麦克风代采主线

- [x] 明确新的语音主入口：当前版本仍以 `按住设备说话` 为主交互动作，实际音频采集由 `电脑麦克风` 完成，不再依赖 ESP32 麦克风直采。
- [x] 新增一个本地电脑录音采集端，优先建议放在 `server/tools/`，避免先引入新的大前端工程。
- [x] 录音采集端至少支持：
  - [x] 由设备按住动作触发的 push-to-talk
  - [x] 开始/停止录音
  - [x] 16kHz / 16bit / mono PCM 输出
  - [x] 直接把 PCM 流送进现有语音链路
- [x] 复用现有 `ASRService`，不要再分叉出第二套识别流程。
- [x] 给语音输入补明确的交互与采集标识，至少能区分 `interaction_surface=device_press / app_text` 与 `capture_source=desktop_mic / device_mic`。
- [x] 设备侧必须提供稳定的 `listening / transcribing / responding` 状态反馈，让用户感知是设备在听和回应。
- [x] 语音入口打通后，至少保留一种稳定输出方式：
  - [ ] 优先方案 A：电脑本地扬声器播放 TTS，同时设备显示回复与状态
  - [x] 备选方案 B：设备负责文字与状态反馈，语音暂不播
  - 回填位：当前实际稳定输出方式 = `B`
- [x] 如果当前阶段不做电脑本地扬声器播放，就必须在文档和 UI 中明确“当前闭环为设备按住触发 + 识别 + 文字/屏幕反馈”。

### P0-2 前端真实可用性补齐

- [x] 修正前端语音能力判断，不要再让 UI 把 `按住设备说话` 展示为可用但实际仍调用 stub；设备在线、电脑麦克风就绪、状态反馈可用性要分层展示。
- [x] 在 Chat 页面补齐会话创建、会话切换、当前会话可视化入口。
- [x] 在 Tasks / Events 页面补齐最小可用 CRUD：
  - [x] 新增
  - [x] 编辑
  - [x] 删除
  - [x] 刷新与错误提示
- [x] 在 Control Center 页面补齐设备命令入口：
  - [x] volume
  - [x] led brightness / color
  - [x] wake / sleep
  - [x] mute
- [x] 把 Notifications / Reminders 从纯展示改成可操作列表，至少支持已读、删除、启停。
- [ ] 升级本机 Flutter/Dart 到满足 `app/pubspec.yaml` 的版本，先恢复 `flutter pub get`、分析和测试能力。
  - 当前状态：`Flutter 3.41.6 / Dart 3.11.4`、`flutter pub get` 成功、`flutter analyze` 无问题；测试仍按仓库规则等待用户确认后再跑。

### P0-3 后端产品闭环补齐

- [x] 给“设备按住触发 + 电脑麦克风代采”选择正式接入方式：
  - [ ] 方案 A：复用 `/ws/device` 作为本地语音客户端接入
  - [x] 方案 B：新增面向本地语音客户端的独立 WebSocket / HTTP 上传入口
  - 回填位：当前正式接入方式 = `B`
- [x] 无论选哪种方式，都要把电脑语音采集与 ESP32 硬件麦克风是否可用解耦；设备可以负责触发与状态反馈，但不能再让本地麦克风失效阻塞主线。
- [x] 给提醒系统接入真实调度，不要停留在 JSON CRUD。
- [x] 让 Reminder 触发后能至少落到一种真实结果：
  - [x] app 事件流通知
  - [ ] 本地桌面提醒
  - [ ] WhatsApp/其他渠道转发
- [x] 明确 `wake_word`、`auto_listen` 当前是否生效，未生效就不要在产品文案中当作已实现能力。
- [x] 为电脑麦克风链路补异常处理：
  - [x] 设备不可用时不影响 ASR
  - [x] ASR 失败时有降级提示
  - [x] TTS 失败时退回文字响应

### P0-4 联调与验收

- [x] 建一份新的语音闭环验收表，按“按住设备说话 + 电脑麦克风代采”主线重新定义验收。
  - 验收文档：`功能讨论区/2026-04-06-P0语音闭环验收记录-按住设备说话+电脑麦克风代采.md`
  - 回填位：联调日期 / 记录人 / 当前输出方式 / 最终结论
- [ ] 至少完成以下端到端检查：
  - [ ] 按住设备说话
  - [ ] 设备进入收音态
  - [ ] 服务端收到 PCM
  - [ ] ASR 成功转文字
  - [ ] 设备进入识别/处理中反馈态
  - [ ] Agent 返回回复
  - [ ] 设备成功显示或播报回复
  - [ ] 至少一种形式完成播报或提示
- [ ] 把联调结果记录回 `功能讨论区/`，避免后续继续沿用旧的硬件麦克风假设。
  - 回填位：日志 / 截图 / 录屏 / 问题单路径

### P0-5 硬件协同最小可用模式

- [x] 明确定义当前硬件角色：
  - [x] `按住设备说话` 的主交互入口
  - [x] `listening / transcribing / responding` 状态反馈终端
  - [x] 显示屏/状态栏终端
  - [x] 在线状态展示
  - [x] 设备命令执行对象
  - [x] 物理交互扩展端
- [x] 在硬件麦克风失效期间，仍保留“按住设备说话”为主交互文案，但要明确其背后由 `电脑麦克风` 负责实际采音。
- [x] 如果设备继续在线，优先保证以下能力可用：
  - [x] 按住时给出明确收音反馈
  - [x] 屏幕显示 AI 文本回复
  - [x] 时间/天气/设备状态稳定刷新
  - [x] 接收前端发起的设备控制命令

## P1：产品功能补齐 TODO

### P1-1 智能待办 / 提醒 / 日历

- [ ] 语音直接生成 Task / Reminder / Event。
- [ ] Task、Reminder、Event 之间建立联动，而不是三套互相独立的数据。
- [ ] 重复提醒、延后提醒、完成提醒、过期提醒做成真实调度行为。
- [ ] `todo_summary`、`calendar_summary` 从“手动读写接口”升级为可自动刷新摘要。
- [ ] 接入外部日历同步前，先完成本地冲突检测和时间排序。

### P1-2 聊天产品化

- [ ] 完整多会话管理。
- [ ] 会话标题自动生成。
- [ ] 会话 pin/archive。
- [ ] 对不同消息来源做清晰标识：
  - [ ] 设备按住说话
  - [ ] 设备语音直采
  - [ ] App 文本
  - [ ] 外部渠道
- [ ] 内部诊断字段再补充真实采音源区分：`desktop_mic / device_mic`。

### P1-3 设备控制产品化

- [ ] 把现有底层 `filesystem / shell / exec` 能力整理成更安全、更可验证的产品化操作集合。
- [ ] 为“电脑控制”设计最小可验收场景：
  - [ ] 打开应用
  - [ ] 打开文件
  - [ ] 执行固定脚本
  - [ ] 查询系统信息
- [ ] 给每个能力补清晰权限边界和失败反馈。

### P1-4 通知与跨渠道

- [ ] 手机推送通知方案定下来。
- [ ] 后端通知中心区分来源、优先级、已读状态、清理策略。
- [ ] 统一 App 通知、Reminder 触发通知、设备通知、WhatsApp 转发策略。

### P1-5 场景模式与人格系统骨架

- [ ] 定义至少 3 个场景模式：
  - [ ] 专注模式
  - [ ] 下班模式
  - [ ] 会议模式
- [ ] 给人格系统补最小配置层：
  - [ ] 语气风格
  - [ ] 回复长度
  - [ ] 主动性
  - [ ] 语音播报风格

## P2：优化、恢复与长期项 TODO

### P2-1 硬件恢复项

- [ ] 单独排查硬件麦克风失效原因：
  - [ ] 焊接
  - [ ] I2S 引脚
  - [ ] 电源噪声
  - [ ] 屏幕刷新干扰
  - [ ] 板级损坏
- [ ] 复跑现有 `firmware/arduino/test12_mic_diag/` 诊断草图并记录结果。
- [ ] 硬件麦克风修好后，再决定是否把实际采音从 `电脑麦克风代采` 切回 `设备本地麦克风直采`。
- [ ] 如果未来恢复设备本地麦克风直采，要求与电脑麦克风代采并存，而不是互相覆盖。

### P2-2 固件能力恢复

- [ ] 给 `firmware/arduino/demo/demo.ino` 补回服务端二进制音频播放能力。
- [ ] 补 shake 上报和对应互动玩法。
- [ ] 把触摸交互从 demo 逻辑升级为可配置交互映射。
- [ ] 根据新的降级模式，考虑增加“设备按住说话（电脑代采）/设备本地麦克风直采/仅显示控制”三种运行状态。

### P2-3 测试与稳定性

- [ ] 前端补 provider / service / websocket / 页面交互测试。
- [ ] 后端给提醒调度、电脑麦克风入口、设备命令异常路径补测试。
- [ ] 把语音链路手测步骤固化成回归脚本或操作文档。
- [ ] 对 `server/services/app_runtime.py` 做模块拆分，降低单文件风险。

### P2-4 工程与体验优化

- [ ] 连接体验补自动发现或最近连接列表增强。
- [ ] Settings 页面不要只暴露字段，要补“是否真实生效”的状态反馈。
- [ ] 给设备在线/离线、ASR不可用、TTS不可用做统一状态提示。
- [ ] 补部署文档，明确：
  - [ ] `按住设备说话（电脑代采）` 模式如何启动
  - [ ] 纯后端模式如何启动
  - [ ] `设备本地麦克风直采` 模式如何启动

## 建议执行顺序

### 第一阶段

- 先完成 `按住设备说话 -> 电脑麦克风代采 -> ASR -> Agent -> 文本回复 -> 设备反馈`。
- 再确定回复输出是走 `电脑扬声器` 还是先只做 `文字+屏幕`。

### 第二阶段

- 补齐前端会话、任务、提醒、设备控制的最小操作闭环。
- 补提醒调度和通知触发。

### 第三阶段

- 恢复硬件麦克风与设备播报。
- 补场景模式、人格系统、日历同步、手机推送。

## 一句话结论

当前项目不是“基础没搭好”，而是已经进入了 `主干已通、产品闭环未完成、设备交互与采音实现需要重新拆分定义` 的阶段。现阶段最重要的不是继续纠结旧方案，而是先围绕 `按住设备说话 + 电脑麦克风代采 + 当前前后端 + 可降级硬件协同` 把真正可用的产品闭环做出来。
